[tool.poetry]
name = "neurai-explorer"
version = "0.1.0"
description = ""
authors = ["zjandali <yzjandali@berkeley.edu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.1.0"
flask-cors = "^5.0.0"

package-mode = false
[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
